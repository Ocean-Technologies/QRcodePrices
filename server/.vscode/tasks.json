{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "rebuild:docker",
      "command": "yarn",
      "args": ["rebuild:docker"],
      "isBackground": true,
      "problemMatcher": {
        "pattern": {
          // We need to write any regex that will never match here, since we're not searching for errors during the task run
          "regexp": "fooooooooooobaaaaaaaaaaaaarrrrrrr",
          "file": 1,
          "location": 2,
          "severity": 3,
          "code": 4,
          "message": 5
        },
        // We need this to tell vs code that the task execution is over and allow it to open google chrome for debug
        "background": {
          "activeOnStart": true,
          "beginsPattern": ".",
          "endsPattern": "."
        }
      }
    },
    {
      "label": "start:docker",
      "command": "yarn",
      "args": ["start:docker"],
      "isBackground": true,
      "problemMatcher": {
        "pattern": {
          // We need to write any regex that will never match here, since we're not searching for errors during the task run
          "regexp": "fooooooooooobaaaaaaaaaaaaarrrrrrr",
          "file": 1,
          "location": 2,
          "severity": 3,
          "code": 4,
          "message": 5
        },
        // We need this to tell vs code that the task execution is over and allow it to open google chrome for debug
        "background": {
          "activeOnStart": true,
          "beginsPattern": ".",
          "endsPattern": "."
        }
      }
    },
    {
      "label": "stop:docker",
      "command": "yarn",
      "args": ["stop:docker"],
      "isBackground": true,
      "problemMatcher": {
        "pattern": {
          // We need to write any regex that will never match here, since we're not searching for errors during the task run
          "regexp": "fooooooooooobaaaaaaaaaaaaarrrrrrr",
          "file": 1,
          "location": 2,
          "severity": 3,
          "code": 4,
          "message": 5
        },
        // We need this to tell vs code that the task execution is over and allow it to open google chrome for debug
        "background": {
          "activeOnStart": true,
          "beginsPattern": ".",
          "endsPattern": "."
        }
      }
    }
  ]
}